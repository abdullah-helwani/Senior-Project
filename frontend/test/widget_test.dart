// Default widget test kept as a smoke placeholder.
// Actual app-level tests live under test/core, test/features, test/helpers.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke', () {
    expect(1 + 1, 2);
  });
}
